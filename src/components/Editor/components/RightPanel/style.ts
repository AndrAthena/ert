import { makeStyles } from '@material-ui/core';

export default makeStyles((theme) => ({
  rightPanel: {
    display: 'flex',
    flexDirection: 'row-reverse',
  },
  panel: {
    flex: '0 0 300px',
    backgroundColor: '#fff',
  },
}));
