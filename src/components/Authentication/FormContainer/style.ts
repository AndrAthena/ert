import { makeStyles } from '@material-ui/core';

export default makeStyles((theme) => ({
  formContainer: {
    width: '100%',
    maxWidth: 420,
  },
  logo: {
    fontSize: 50,
    marginBottom: theme.spacing(1),
  },
}));
