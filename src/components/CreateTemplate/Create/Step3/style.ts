import { makeStyles } from '@material-ui/core';

export default makeStyles((theme) => ({
  step3: {
    maxWidth: '80%',
    marginLeft: 'auto',
    marginRight: 'auto',
    marginBottom: theme.spacing(2),
  },
}));
